Like with any install, the first step is to update the APT cache.  
This is done with the command apt-get update.

# TASK

To complete this step, update the APT cache.
`apt-get update' {{execute}}{{verify}}
